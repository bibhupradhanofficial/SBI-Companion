# Netlify configuration file
# This configuration tells Netlify to redirect all routes to index.html 
# so that react-router-dom can handle client-side routing.

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
